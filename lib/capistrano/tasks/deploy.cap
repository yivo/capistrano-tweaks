namespace :deploy do
  desc 'Deploy project first time'
  task :initial do
    if Rake::Task.task_defined?('deploy:migrate')
      Rake::Task['deploy:migrate'].clear
    end

    after 'deploy:updated', 'rails:database:setup'
    invoke 'deploy'
  end
end